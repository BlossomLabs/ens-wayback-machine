{
  "name": "wayback-machine-monorepo",
  "private": true,
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "format-and-lint": "biome check .",
    "format-and-lint:fix": "biome check . --write",
    "prepare": "husky"
  },
  "devDependencies": {
    "@biomejs/biome": "^1.8.3",
    "husky": "^9.1.5",
    "turbo": "^2.1.1"
  },
  "engines": {
    "node": ">=18"
  },
  "packageManager": "bun@1.0.3",
  "workspaces": ["apps/*", "packages/*"]
}
